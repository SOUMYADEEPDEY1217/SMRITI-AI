version: '3.8'

services:
  backend:
    build: .
    container_name: smriti_ai_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    restart: unless-stopped
